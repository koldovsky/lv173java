<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security.xsd">

	<!-- enable use-expressions -->
	<http auto-config="true" use-expressions="true">
	
 		<intercept-url pattern="/adminregister" access="hasRole('ROLE_ADMIN')" /> 
		<intercept-url pattern="/agency" access="hasAnyRole('ROLE_REDADMIN', 'ROLE_REDADMIN')" />
		<intercept-url pattern="/redadmin" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/api/**" access="hasAnyRole('ROLE_REDADMIN', 'ROLE_ADMIN')" />
		<intercept-url pattern="/checkUnique" access="hasAnyRole('ROLE_REDADMIN', 'ROLE_REDADMIN')" />

		<!-- access denied page -->
		<access-denied-handler error-page="/index" />
		<form-login login-page="/login" default-target-url="/index"
			login-processing-url="/j_spring_security_check" 
			authentication-failure-url="/login?error" 
			username-parameter="email"
			password-parameter="password" />
		<csrf disabled="true"/>
	</http>

	<beans:bean id="UserDetailsServiceImplementation"
		class="com.softserveinc.ita.redplatform.business.service.UserDetailsServiceImplementation">
		<beans:property name="userService" ref="userService"></beans:property>
	</beans:bean>
	
	<beans:bean id="userFilter" class="com.softserveinc.ita.redplatform.web.filters.UserFilter">
		<beans:property name="userService" ref="userService"></beans:property>
	</beans:bean>
	
	<beans:bean id="bcryptEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder" />
 
	
	<!-- Select users and user_roles from database -->
	<authentication-manager>
		<authentication-provider user-service-ref="UserDetailsServiceImplementation">
		 <password-encoder ref="bcryptEncoder"/>
		</authentication-provider>
	</authentication-manager>
	
	<global-method-security secured-annotations="enabled"/>

</beans:beans>